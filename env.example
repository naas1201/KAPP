# KAPP Environment Variables
# Copy this file to .env.local and fill in your values
#
# For Cloudflare deployment, most of these variables should be set in the
# Cloudflare Dashboard under Workers & Pages → Settings → Variables
# See: docs/CLOUDFLARE_DEPLOYMENT.md for complete setup instructions

# =============================================================================
# STRIPE PAYMENT CONFIGURATION
# =============================================================================
# Get your keys from: https://dashboard.stripe.com/apikeys

# Publishable key (safe for client-side, starts with pk_)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Secret key (server-side only, starts with sk_)
# For Cloudflare: Set as encrypted secret in Dashboard
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# =============================================================================
# FIREBASE CONFIGURATION
# =============================================================================
# Firebase Auth is used for authentication (kept in Cloudflare deployment)
# Get these values from Firebase Console → Project Settings → Your apps

NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# =============================================================================
# CLOUDFLARE CONFIGURATION (For Cloudflare Deployment)
# =============================================================================
# Find your Account ID at: dash.cloudflare.com → Right sidebar → Account ID
# Or in the URL: dash.cloudflare.com/[ACCOUNT_ID]/...

CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id

# R2 Storage API credentials (only needed if using R2 API directly)
# Create at: dash.cloudflare.com → R2 → Manage R2 API Tokens
# For Cloudflare: Set as encrypted secrets in Dashboard
# R2_ACCESS_KEY_ID=your_r2_access_key
# R2_SECRET_ACCESS_KEY=your_r2_secret_key

# =============================================================================
# LOCAL DEVELOPMENT
# =============================================================================
# For local development with Firebase emulators:
# FIRESTORE_EMULATOR_HOST=localhost:8080
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099

# =============================================================================
# AI CONFIGURATION (Optional)
# =============================================================================
# For local development with Google GenKit (optional)
# In Cloudflare deployment, Workers AI is used instead
# GOOGLE_API_KEY=your_google_ai_api_key
